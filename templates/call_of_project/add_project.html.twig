{% extends 'top_navbar_layout.html.twig' %}

{% block title %}
    {{ 'app.call_of_project.add_project.h1'|trans }}
{% endblock %}

{% block content %}
    <h1 class="mb-4">{{ 'app.call_of_project.add_project.h1'|trans }}</h1>

    <div class="card">
        <div class="card-body">
            <h2>{{ call_of_project.name }}</h2>
            {{ dynamic_form_html|raw }}
        </div>
    </div>
{% endblock %}

