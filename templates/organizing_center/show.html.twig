{% extends 'base.html.twig' %}

{% block title %}OrganizingCenter{% endblock %}

{% block body %}
    <h1>OrganizingCenter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ organizing_center.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ organizing_center.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ organizing_center.status }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ organizing_center.createdAt ? organizing_center.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ organizing_center.updatedAt ? organizing_center.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('organizing_center_index') }}">back to list</a>

    <a href="{{ path('organizing_center_edit', {'id': organizing_center.id}) }}">edit</a>

    {{ include('organizing_center/_delete_form.html.twig') }}
{% endblock %}
