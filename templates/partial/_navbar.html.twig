<nav class="main-header navbar navbar-expand navbar-white navbar-light">

    {% set isSidebarLayout =  layout_class == layout_sidebar_class %}

    {% set wrapper = isSidebarLayout ? null : container %}
    {% apply wrapper_container(wrapper) %}


        <!-- Left navbar links -->
        <ul class="navbar-nav">
            {% if isSidebarLayout %}
                <li class="nav-item">
                    <a class="nav-link" data-widget="pushmenu" href="#" role="button">
                            <i class="fas fa-bars"></i>
                    </a>
                </li>
            {% else %}
                {{ include('partial/_logo.html.twig') }}
            {% endif %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app.homepage') }}">
                    {{ 'app.navbar.item.dashboard.label'|trans }}
                </a>
            </li>
        </ul>

    {% endapply %}


</nav>